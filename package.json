{
  "name": "site",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "dev": "pnpm -r dev",
    "watch": "pnpm -r watch"
  }
}
